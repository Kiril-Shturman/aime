# Цикл: как агент ведёт любой проект

Инструкция написана так, чтобы годиться для любого проекта на доске: игры,
сайта, бота, ролика. Про конкретный проект агент узнаёт из самой доски —
из описания, репозитория, ролей участников и этапов роудмапа. Ничего
проектного зашивать сюда не надо.

## Когда просыпаться

Расписание — не главный будильник, а страховка. Работает так:

- **Событие.** Человек поставил цель или задачу — доска будит агента сразу,
  ждать нечего.
- **Проход.** За один заход берём до трёх задач подряд: сделал, отчитался,
  взял следующую. Кончились задачи активного этапа — заканчиваем.
- **Сердцебиение.** Раз в шесть часов на всякий случай: вдруг что-то повисло
  или изменилось снаружи. Обычно этот проход заканчивается тишиной.

Час между проходами — плохая идея: если работы нет, это пустая трата, а если
есть, глупо ждать целый час между двумя соседними задачами.

## Проход по шагам

**1. Осмотреться.** `board_overview`. Смотрим, есть ли этапы в работе и есть
ли задачи, которые уже кто-то взял.

**2. Подобрать хвосты.** Если висит задача в статусе «в работе» дольше суток и
это твоя задача — либо доводи её, либо верни в «не начата» с отчётом, почему
не пошло. Брошенная в работе задача блокирует цикл.

**3. Выбрать цель.** Работаем только с этапом в статусе «в работе». Если таких
нет — ничего не выдумываем: пишем владельцу одну строку «активных этапов нет,
скажи, за что браться» и заканчиваем проход.

**4. Разложить цель на задачи.** Цель ставится словами: человек пишет абзац
вроде «провести турнир на 100 игроков со входом 10 звёзд», и это становится
активным этапом. Никаких полей и статусов он не заполняет — разбор твоя работа.

Если у активного этапа задач нет или меньше трёх, а цель явно больше —
заведи недостающие одним вызовом `board_split_goal` (пачкой, а не по одной).
Правила разбора:
- одна задача = один законченный кусок работы на 10–40 минут;
- формулировка в повелительном наклонении и проверяемая: не «улучшить
  дизайн», а «сделать пустые экраны с подсказкой вместо слова „Пусто“»;
- не больше семи задач за раз, иначе доска превращается в свалку;
- если непонятно, из чего состоит цель, — не выдумывай, спроси владельца;
- слишком крупный кусок дроби дальше: у задачи есть родитель, подзадачи
  показываются под ней с отступом.

Задача-обёртка вроде «доделать проект» — не задача, а цель: разложи её и
закрой отчётом о разборе, а работу веди по получившимся кускам.

**5. Взять задачу.** `board_next_task`, затем `board_take`. Взял — она твоя,
второй проход её не подберёт. Сделал и отчитался — возвращайся к шагу 5,
пока не сделаешь три задачи или пока свободные не кончатся.

**6. Сделать.** Где лежат исходники, сказано в карточке проекта: поле
«исходники» — готовая папка на сервере, поле «репозиторий» — если папки нет,
клонируй сам в `~/projects/<имя>` ключом `~/.ssh/id_ed25519_tasks` и работай
там. Не нашёл ни того, ни другого — не выдумывай, спроси владельца.

Правки в исходниках доски видны сразу: служба `board` запущена из этой же
папки, достаточно `systemctl --user restart board`.

Общие правила:
- ветка отдельная, в main не пушим;
- ничего не удаляем без спроса;
- деньги, рассылки, публикации, удаление данных — только с подтверждения
  владельца, даже если задача звучит как разрешение.

**7. Отчитаться.** `board_report`: что сделано, что осталось, ссылка на
результат (коммит, ветка, файл). Отчёт пишем всегда — и когда получилось,
и когда нет. Закрываем задачу (`done: true`) только если результат можно
предъявить. Не получилось — отчёт с причиной и статус обратно в «не начата».

**8. Закрыть этап.** Если все задачи этапа закрыты, переведи этап в `done`
через `board_stage_status` и напиши владельцу одну строку: этап такой-то
закрыт, вот что в нём было.

**9. Замолчать.** Если делать нечего — не пиши ничего. Тишина это нормальный
результат прохода. Сообщение владельцу отправляем только когда есть повод:
закрыл задачу, закрыл этап, застрял, нужно решение.

## Границы

- За проход берём **до трёх** задач. Не «пока не кончатся».
- Не трогаем проекты, у которых нет активного этапа.
- Не переписываем чужие задачи и не удаляем их — только свои отчёты.
- Если два прохода подряд задача не двигается — не долбись, вынеси вопрос
  владельцу.
- Любое сомнение решается в пользу «спросить», а не «сделать».

## Как это выглядит на живом примере

Этап «Доска задач» в проекте aiMe, в нём одна задача «Доделать доску».
Проход первый: задач мало для такой цели — раскладываем на «пустые экраны»,
«анимация вычёркивания», «расписание цикла», «навык под типовую работу».
Проход второй: берём первую, делаем, коммитим в ветку, пишем отчёт.
Проход третий: берём следующую. И так, пока этап не закроется сам собой.

Тот же текст сработает для «Коридора» — там своя цель, свой репозиторий и
свои роли, а порядок действий ровно этот.
