# Доска задач

Мини-аппа Телеграма в духе «Напоминаний» Apple: тёмный экран, плитки-сводки,
проекты с командой и роудмапом. Задумана как **состояние** для агента —
он читает и закрывает задачи через API, а человек смотрит ту же доску глазами.

## Что внутри

- `server.py` — aiohttp на порту 8095, хранилище в `data.json` (создаётся сам).
- `static/index.html` — весь интерфейс одним файлом: разметка, стили, логика.
- `avatars.py` — тянет аватарки телеграм-ботов с их публичных страниц `t.me`
  (токены не нужны) и складывает в `static/avatars/`.

## Запуск

```bash
pip3 install aiohttp
./run.sh                     # http://localhost:8095, с пинком агенту
python3 server.py            # без пинка, просто доска
python3 avatars.py           # подтянуть аватарки участников-ботов
```

## Модель данных

```
проект   { id, name, color, note, repo, members[], roadmap[] }
участник { id, name, handle, role, kind: bot|agent|service|human, avatar }
этап     { id, title, date, status: planned|active|done, progress {done,total} }
задача   { id, title, note, report, url, project, stage, parent, member,
           status: todo|doing|done, commit, tokens, seconds,
           started_at, done_at, due, time, flagged, done }
```

У проекта есть карточка репозитория: подключаешь адресом, доска сама проверяет
доступ через `git ls-remote`, клонирует в `~/projects/<имя>` и запоминает путь.
Дальше в карточке видно ветку, последний коммит и число незакоммиченных правок.

Задача привязана к этапу — у одного этапа их бывает десяток. Отсюда прогресс
этапа «3 из 7» и группировка задач под этапами на экране проекта.

Участник — не обязательно телеграм-бот: это может быть ИИ-агент, сервис или
человек. Роль пишется словами и видна в строке.

## API

| Метод | Путь | Что делает |
|---|---|---|
| GET | `/api/state` | сводка, проекты, задачи одним ответом |
| POST | `/api/goal` | `{text, project}` — цель словами, заводит активный этап |
| POST | `/api/tasks` | `{project, stage, parent, items[]}` — разбор пачкой |
| POST | `/api/task` | `{title, note, url, project, stage, member, due, time, flagged}` |
| PATCH | `/api/task/<id>` | `{title, note, status, stage, member, report, commit, tokens, seconds, …}` |
| POST | `/api/task/<id>/toggle` | отметить выполненной и обратно |
| DELETE | `/api/task/<id>` | удалить |
| POST | `/api/project` | `{name, color, note, repo, members[]}` |
| PATCH | `/api/project/<id>` | `{name, color, note, repo}` |
| DELETE | `/api/project/<id>` | вместе с задачами |
| GET | `/api/project/<id>/git` | ветка, последний коммит, сколько правок не закоммичено |
| POST | `/api/project/<id>/git` | `{repo, branch}` — проверить доступ, склонировать, запомнить путь |
| POST | `/api/project/<id>/member` | `{name, handle, role, kind}` |
| DELETE | `/api/project/<id>/member/<mid>` | убрать участника |
| POST | `/api/project/<id>/stage` | этап роудмапа |
| PATCH | `/api/project/<id>/stage/<sid>` | сменить статус или текст |
| DELETE | `/api/project/<id>/stage/<sid>` | убрать этап |

## Коннектор для агента

`mcp_board.py` — MCP-сервер поверх stdio, через который агент видит доску.
Зависимостей нет, только стандартная библиотека: на сервере с гигабайтом
памяти лишнее ни к чему.

```bash
scp mcp_board.py сервер:/home/ubuntu/board-mcp.py
openclaw mcp add board --command python3 --arg /home/ubuntu/board-mcp.py \
  --env BOARD_URL=http://127.0.0.1:8095
openclaw mcp probe        # должно показать: board: 6 tools
```

Инструменты подобраны под цикл, а не под полноту API:

| Инструмент | Зачем |
|---|---|
| `board_overview` | что горит: проекты, этапы с прогрессом, что в работе |
| `board_next_task` | выдать следующую свободную задачу (активный этап → флажок → срок) |
| `board_take` | взять задачу в работу, чтобы второй проход её не подобрал |
| `board_report` | записать отчёт и при необходимости закрыть |
| `board_add_task` | завести задачу, при желании под этапом |
| `board_set_goal` | принять цель, сформулированную словами |
| `board_split_goal` | разложить цель на задачи и завести их пачкой |
| `board_stage_status` | двинуть этап роудмапа |
| `board_bot_list` | какими телеграм-ботами доска умеет управлять |
| `board_bot_send` | написать от имени подключённого бота |
| `board_bot_call` | любой метод Bot API от его имени |
| `board_dm_ask` | написать боту от имени владельца и дождаться ответа |
| `board_dm_send` | просто отправить сообщение в личку |
| `board_dm_read` | последние сообщения переписки |

## Телеграм-боты проекта

Участник вида «Бот» — это не просто строка в списке: его можно подключить
по-настоящему. В карточке участника есть поле «Ссылка или токен»:

- кинул `t.me/MyBot` — доска запомнила имя и ник, не более того;
- кинул токен от `@BotFather` — доска проверила его через `getMe` и с этого
  момента ходит в Bot API от имени бота.

Токен лежит в `bots.json` рядом с данными, права `600`, наружу не отдаётся:
в `/api/state` у участника видно только `{connected, username, name, bot_id}`.
Дальше ботом распоряжается агент — словами, через `board_bot_send` и
`board_bot_call`:

```
напиши от Игры в чат -1001234567890, что турнир начинается в семь
```

**Входящие доска не читает.** `getUpdates` и вебхук у бота ровно один: заберёшь
себе — у живого бота отвалится собственный опрос. Поэтому `getUpdates`,
`setWebhook`, `deleteWebhook`, `close` и `logout` запрещены на уровне API
доски. Наше дело — исходящие вызовы: сообщения, меню, команды, картинки.

## Промты другим ботам

Пользоваться чужими ботами (нейросети, сервисы) через своего бота нельзя:
Телеграм отвечает `USER_BOT_TO_BOT_DISABLED` — бот боту не пишет, и обойти это
нечем. Разговор с ботом заводит только живой аккаунт.

Поэтому рядом с доской живёт `userbot.py` — клиент на Telethon, работающий от
имени владельца:

Вход делается из самой доски: Профиль → «Аккаунт для промтов» → api_id и
api_hash с my.telegram.org, потом телефон и код. Код вводится там же и никуда
не пересылается — Телеграм аннулирует коды, замеченные в переписке.

Из командной строки то же самое:

```bash
pip3 install --user telethon
echo "TG_API_ID=...\nTG_API_HASH=..." > userbot.env   # ключи с my.telegram.org
python3 userbot.py code +79990000000                  # придёт код входа
python3 userbot.py enter "1 2 3 4 5"                  # код, при нужде пароль 2FA
python3 userbot.py ask @some_ai_bot "напиши план поста"
```

Доска зовёт его сама: `POST /api/dm/ask {to, text, wait}` отправляет промт и
ждёт, пока бот допишет ответ (нейросети правят одно и то же сообщение, поэтому
ждём, пока текст перестанет меняться). Агент делает то же самое инструментом
`board_dm_ask`.

Сессия лежит в `userbot.session` с правами `600` и в гит не ходит: это доступ к
аккаунту целиком. Рассылать с него что попало нельзя — Телеграм ограничивает
аккаунты за спам, а отвечать будет живой человек, а не бот.

## Мини-аппа в Телеграме

Доска живёт на сервере как служба `board` (systemd, порт 8095), снаружи её
отдаёт Caddy с автоматическим сертификатом:

```
https://board.143-47-186-106.nip.io
```

Имя вида `nip.io` разрешается в IP само, поэтому домен покупать не пришлось,
а Телеграму нужен именно HTTPS. Кнопка меню бота переключена с длинного списка
команд на мини-аппу:

```bash
curl -X POST "https://api.telegram.org/bot<TOKEN>/setChatMenuButton" \
  -d '{"menu_button":{"type":"web_app","text":"Доска",
       "web_app":{"url":"https://board.143-47-186-106.nip.io/"}}}'
```

Сами команды переехали внутрь приложения: в меню три точки есть «Прогнать
цикл», «Статус агента» и «Ещё команды». Список закрытый (`COMMANDS` в
`server.py`), ответ показывается в приложении.

## Когда просыпается агент

Расписание — страховка, а не главный будильник:

- поставил цель или задачу → доска сама будит агента (`LOOP_TRIGGER_CMD`,
  записи самого агента помечены заголовком `X-Actor: agent` и пинок не вызывают);
- за проход агент делает до трёх задач подряд, потом останавливается;
- раз в шесть часов — сердцебиение на случай зависшего или внешнего.

## Чего пока нет

Состояние есть, коннектор есть — агент читает доску и пишет в неё.
Для замкнутого цикла осталось: расписание (никто агента не будит),
подагенты под роли участников и навыки — записанные инструкции, как мы
делаем типовую работу.
