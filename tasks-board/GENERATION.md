# Как агент выполняет заказы с экрана «Генерация»

Человек нажимает в мини-аппе «Создать фото», «видео», «пост» или «аудио»,
описывает словами, что нужно, и заказ ложится задачей в проект — с флажком,
чтобы попасть в работу первым. В названии стоит тип, в примечании — полное
описание. Дальше работа твоя.

## Фото — умеем сами

```bash
set -a; . ~/.config/openclaw.env; set +a
python3 ~/tools/gen_image.py "описание кадра" /tmp/out.png [референс1.jpg …]
```

Модель — Nano Banana Pro через OpenRouter. Одна картинка ≈ 22 секунды и
**$0.14**, поэтому генерируем обдуманно: сначала формулируем промпт целиком,
потом запускаем. Не устраиваем перебор вариантов без просьбы.

Референсы необязательны: если в заказе есть ссылка на картинку или файл,
скачай и передай их последними аргументами — модель дорисует по образцу.

Готовое отправляем владельцу:

```bash
T=$(python3 -c "import json;print(json.load(open('/home/ubuntu/.openclaw/openclaw.json'))['channels']['telegram']['botToken'])")
curl -s -F chat_id=341730072 -F photo=@/tmp/out.png -F caption="описание" \
  "https://api.telegram.org/bot$T/sendPhoto" > /dev/null
```

В отчёте по задаче пишем, что отправили, и ставим цену: `tokens` не при чём,
но стоимость картинки упоминаем словами — «$0.14, одна картинка».

## Пост — тоже сами

Текст пишем сами и кладём прямо в отчёт задачи, чтобы его было видно на
доске. Если просят длинный материал — файлом в `/tmp` и отправляем
`sendDocument` тем же способом.

## Видео и аудио — пока не умеем

На сервере нет ни синтеза речи, ни рендера видео: гигабайт памяти и никаких
ключей к таким моделям. Не выдумывай обходные пути и не обещай сделать
позже. Верни задачу в «не начата» и напиши владельцу одну строку: «видео
пока сделать нечем, нужен доступ к такому-то сервису».

Исключение: если в команде проекта есть исполнитель, который работает на
машине владельца (Claude Code, курсор), — назначь задачу ему, там есть и
MoneyPrinterTurbo, и всё остальное.

## Общее

- Не начинай генерацию, если описание пустое или непонятное: спроси.
- Деньги тратим только на то, что заказано. Одна задача — одна попытка;
  вторая попытка только если владелец попросил переделать.
